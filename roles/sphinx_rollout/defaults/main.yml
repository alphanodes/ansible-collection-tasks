---
# if not specified, all instances specified in sphinx_instances are used

sphinx_instances: []
sphinx_rollout_instance: ''
sphinx_base_dir: /srv/sphinx
sphinx_build_prod: _build_prod
sphinx_always_build: true
